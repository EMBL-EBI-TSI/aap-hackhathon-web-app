export interface Domain {
  domainId : number;
  domainName : string;
  domainDesc : string;
  domainReference : string;
  adminDomainReference : string; //to capture adminDomain reference for each domain
}
